node_modules
# Prettier walks everything under `..`, git's own store included.
.git
# Git worktrees. Each is a checkout with its own .prettierrc.yaml, whose
# relative plugin path points at a node_modules that was never installed.
.claude
frontend/dist
.research
.venv
runtime
compose.override.yaml
docs/brief.md
uv.lock
frontend/playwright-report
frontend/test-results
htmlcov
coverage.xml
