# Copy to .env to override deployment defaults.
MUSIMO_BIND=127.0.0.1
MUSIMO_PORT=8765
PUID=1000
PGID=1000
# Use poll for Windows Docker Desktop or network mounts without filesystem events.
MUSIMO_WATCH_MODE=native
MUSIMO_POLL_INTERVAL_SECONDS=60
